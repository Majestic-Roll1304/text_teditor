#include<stdio.h>
#include<stdlib.h>
#include<fcntl.h>
#include<unistd.h>

void readmode(char *name)
{
  int fd=open(name,O_RDONLY);
  return fd;
}
int main(int argc, char **argv)
{
  //first try and open in read mode
  if(readmode(argv[1]))
    {
      printf("the file exists");
    }
}
